<!-- views/feed.hbs -->
{{#> layout}}
<div class="feed-container">
    {{#if service}}
        <div class="video-wrapper">
            <video 
                src="http://localhost:9000/construction-assets/{{service.videoUrl}}" 
                class="feed-video" 
                autoplay 
                muted 
                loop 
                playsinline>
            </video>
            <div class="video-overlay">
                
                <!-- Левая/Нижняя часть: Текст и теги -->
                <div class="feed-content">
                    <h2 class="service-title">{{service.title}}</h2>
                    
                    <!-- Плашки с цифрами под названием (без категории) -->
                    <div class="service-details">
                        <span class="detail-item">{{service.price}} руб/м²</span>
                        <span class="detail-item">{{service.area}} м²</span>
                    </div>
                    
                    <!-- Описание с кнопкой "больше/меньше" внизу -->
                    <div class="description-wrapper">
                        <input type="checkbox" id="desc-toggle" class="desc-checkbox">
                        <div class="service-description">
                            <p class="main-text">{{service.description}}</p>
                            <!-- Дополнительный тематический текст (скрыт по умолчанию) -->
                            <p class="extra-text">В расчет укрупненного показателя стоимости включены: базовые строительно-монтажные работы (СМР), устройство фундамента и подведение наружных инженерных сетей до границы участка.</p>
                        </div>
                        <!-- Сама кнопка -->
                        <label for="desc-toggle" class="toggle-description" data-more="Больше" data-less="Меньше"></label>
                    </div>
                </div>

                <!-- Правая часть: Лайки и Следующий -->
                <div class="feed-sidebar">
                    <div class="sidebar-item">
                        <div class="sidebar-icon">❤️</div>
                        <span class="sidebar-text">{{likesCount}}</span>
                    </div>
                    
                    <!-- Кнопка "Следующий" строго по методичке -->
                    <a href="/construction/feed?id={{service.id}}&next=true" class="sidebar-item next-btn">
                        <div class="sidebar-icon">⬇️</div>
                        <span class="sidebar-text">Следующий</span>
                    </a>
                </div>

            </div>
        </div>
    {{else}}
        <div class="empty-state"><p>Нет доступных услуг</p></div>
    {{/if}}
</div>
{{/layout}}